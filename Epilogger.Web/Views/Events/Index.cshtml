@model List<Epilogger.Web.Models.EventDisplayViewModel>

@{
    ViewBag.Title = "Index";
}

@foreach (var item in Model) {
    <li>@Html.ActionLink(@item.Name, "Details", "Events", new { id = @item.ID }, null)</li>
}