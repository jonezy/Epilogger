@model System.TimeSpan?
@Html.TextBox("", Model.HasValue ? new DateTime(Model.Ticks).ToString("h:mm tt") : new DateTime(DateTime.Today.TimeOfDay.Ticks).ToString("h:mm tt"), new { @class = "timedropdown", @maxlength = "8" })
